import 'package:flutter/material.dart';

class ScreenEight extends StatelessWidget {
  const ScreenEight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Screen 8")),
    );
  }
}
