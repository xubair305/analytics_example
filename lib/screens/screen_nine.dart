import 'package:flutter/material.dart';

class ScreenNine extends StatelessWidget {
  const ScreenNine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Screen 9")),
    );
  }
}
